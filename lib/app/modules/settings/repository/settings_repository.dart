class SettingsRepository {
  // Contoh simpan preferensi
  Future<void> saveDarkMode(bool value) async {
    // Simpan ke SharedPreferences atau API
  }

  Future<bool> loadDarkMode() async {
    // Ambil dari SharedPreferences
    return false;
  }
}
